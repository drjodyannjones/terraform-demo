# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain secret data, such as
# password, private keys, and other sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore any .terraform.lock.hcl files that may have been generated
# These lock files may not be necessary to share with others
.terraform.lock.hcl

# Ignore directories created by Terraform plugin cache
.terraform/plugins/
.terraform/providers/

# Ignore any files that may contain sensitive data
secrets.tf
secrets.tfvars
secrets.tfvars.json

# Ignore output of the terraform plan command
*.tfplan
*.tfplan.*

# Ignore log files
*.log

# Exclude directories that may contain vendor files
vendor/
